import { Metadata } from "next";

import CartPage from "@/pages/CartPage";

export default CartPage;

export const metadata: Metadata = {
  title: "Store - Cart",
  description: "Cart Page for our Fake Store",
};
