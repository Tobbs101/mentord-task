import { Metadata } from "next";

import HomePage from "@/pages/HomePage";

export default HomePage;

export const metadata: Metadata = {
  title: "Store - Home",
  description: "Home Page for our Fake Store",
};
